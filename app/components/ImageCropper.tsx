export type ImageCropperProps = { src: string, setSrc: (newSrc: string) => void, open: boolean, setOpen: (state:boolean) => void };


export default function ImageCropper({ src, setSrc, open, setOpen }: ImageCropperProps) {



    return (
        <dialog>

        </dialog>
    )
}